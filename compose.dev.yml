services:
  reverse-proxy:
    extends:
      file: ./compose.yml
      service: reverse-proxy
    networks:
      - local-network
    volumes:
      - /var/www/region-one/:/usr/share/nginx/html/:ro

networks:
  local-network:
    external: true
